import { ProductDataService } from './product.data.service';

describe('ProductDataService', () => {
  it('should create an instance', () => {
    expect(new ProductDataService()).toBeTruthy();
  });
});
